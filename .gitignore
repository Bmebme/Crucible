__pycache__/
*.egg-info/
.pytest_cache/
.venv/

# macOS
.DS_Store

# deploy secrets
deploy/.env
frontend/node_modules/
backend/.venv/

# frontend/dist 构建产物入 git: 内网无 npm registry, git pull + 薄层构建
# 是前端更新的唯一通道 (薄层 COPY frontend/dist, 见 deploy/Dockerfile.crucible)
!frontend/dist/

# 镜像构建上下文 (模型缓存+torch wheel, 280MB, 不入 git)
.hf-bake/

# MCP Host 白名单的本地副本 (开发时自动生成; 容器里在 /data, 不入 git)
mcp_allowed_hosts.txt
